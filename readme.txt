# ETF Analytics
Diese Applikation dient der Kalkulation von finanziellen Metriken (bspw. dem PEG) der Bestandteile eines eingegeben ETFs

## Setup

```bash
python -m venv .venv
source .venv/bin/activate   # On Windows use .venv\Scripts\activate
pip install -r requirements.txt

